
import styled from 'styled-components';

const Wrapper = styled.div`
    .button {
        cursor: pointer;
    }
    input[type=file] {
        visibility: hidden;
        position: absolute;
    }`

export default Wrapper;